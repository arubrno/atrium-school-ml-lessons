# artifacts/

Where the **visualize** skill writes its pages.

Each one is a single self-contained HTML file. Open it by double-clicking, or
from Zed's file tree. Nothing here is needed by anything else — delete freely.

Files in this folder are not committed.
